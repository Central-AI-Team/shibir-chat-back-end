# Python & FastAPI
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment
.venv/
venv/
# Isolated env for scripts/eval_ragas.py's ragas/datasets/langchain deps --
# deliberately NOT the same venv as the app (see that script's docstring).
venv-ragas/
.env

# Vector store (generated by ingest.py)
chroma_db/

# Environment variables
.env

# IDE / editor files
.vscode/
.idea/
*.swp

# OS files
.DS_Store

# LibreOffice/OpenOffice lock files (leaked into git once already -- see
# .~lock.full_dataset.csv#, removed in the uv migration cleanup)
.~lock.*#

# Logs
*.log

# Generated eval reports (scripts/eval_responses.py)
eval_reports/

# Intermediate corpus sample for hand-labelling (scripts/sample_corpus_pages.py)
scripts/_corpus_sample.json
