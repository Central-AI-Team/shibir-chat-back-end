name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []  # no CI job here since push-to-main already passed the PR's CI check
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add your deploy steps here (Vercel, AWS, Docker push, etc.)"